const clock=document.querySelector('#clock');function tick(){clock.textContent=new Date().toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'})}tick();setInterval(tick,1000);
const cur=document.querySelector('.cursor');addEventListener('mousemove',e=>{cur.style.left=e.clientX+'px';cur.style.top=e.clientY+'px'});document.querySelectorAll('a').forEach(a=>{a.addEventListener('mouseenter',()=>{cur.style.width='34px';cur.style.height='34px';cur.style.background='rgba(255,255,255,.08)'});a.addEventListener('mouseleave',()=>{cur.style.width='18px';cur.style.height='18px';cur.style.background='transparent'})});
const c=document.querySelector('#fx'),x=c.getContext('2d');let p=[];function size(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);p=Array.from({length:Math.min(75,Math.floor(innerWidth/18))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.2+.25,v:Math.random()*.18+.04}))}function draw(){x.clearRect(0,0,innerWidth,innerHeight);x.fillStyle='rgba(255,255,255,.42)';for(const q of p){q.y-=q.v;if(q.y<0){q.y=innerHeight;q.x=Math.random()*innerWidth}x.beginPath();x.arc(q.x,q.y,q.r,0,Math.PI*2);x.fill()}requestAnimationFrame(draw)}addEventListener('resize',size);size();draw();